"""Opt-in, clearly-labeled experimental features that make no removal guarantees.

Nothing in this package is wired into the default ``inspect``/``clean``/
``verify`` pipeline. It exists so future, real implementations have a home
without needing to restructure the app.
"""
