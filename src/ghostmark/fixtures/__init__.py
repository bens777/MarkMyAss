"""Synthetic fixture generation for `ghostmark demo` and the test suite.

Every fixture is generated in-process (no bundled binary test files, no
third-party or copyrighted content) so the repository stays clean and the
demo works offline on a fresh checkout.
"""
